# spring-patterns v2.3 캐싱 & 압축

## 프로젝트 개요

### 선행 조건
- v2.0 WebFlux 전환 완료
- v2.1 Virtual Threads 통합 완료
- v2.2 Rate Limiting 완료

### 현재 상태 (v2.2)
- Rate Limiting 구현
- Redis 기반 분산 제한

### 목표 상태 (v2.3)
- **응답 캐싱 & 압축**
- Redis 캐시
- Gzip 압축

## 버전 로드맵 (v2.3 세부화)

| 버전 | 기간 | 설명 |
|------|------|------|
| v2.3.0 | 0.5주 | Redis Cache & Gzip |

---

## v2.3.0: Redis Cache & Compression

### 작업 내용
| 순서 | 작업 | 핵심 파일 |
|------|------|----------|
| 2.3.0.1 | Redis Cache 설정 | `CachingConfig.java` |
| 2.3.0.2 | @Cacheable 적용 | `ProductService.java` |
| 2.3.0.3 | 캐시 무효화 | `CacheEvictionService.java` |
| 2.3.0.4 | Gzip 압축 필터 | `CompressionConfig.java` |

### 커밋 포인트
```bash
git commit -m "feat(cache): configure Redis cache manager with TTL"
git commit -m "feat(service): apply @Cacheable for hot data"
git commit -m "feat(cache): add cache eviction strategies"
git commit -m "feat(filter): add gzip compression for responses"
git tag -a v2.3.0 -m "v2.3.0: Caching & Compression"
```

## 캐시 전략

| 캐시 유형 | TTL | 용도 |
|----------|-----|------|
| product | 30분 | 상품 상세 |
| products | 5분 | 상품 목록 |
| categories | 1시간 | 카테고리 |
| hot-items | 1분 | 인기 상품 |
