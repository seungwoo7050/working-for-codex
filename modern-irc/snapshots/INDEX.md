# 📸 modern-irc Snapshots Index

> 패치별 빌드 가능한 스냅샷 코드 모음

## 개요

이 디렉토리는 `CLONE-GUIDE.md`의 각 패치 시점에 해당하는 빌드 가능한 코드 스냅샷을 포함합니다.
각 스냅샷은 독립적으로 빌드하고 실행할 수 있습니다.

---

## 스냅샷 목록

### v1.0.0 Core Functionality (7개 스냅샷)

| 스냅샷 | 패치 ID | 이름 | 주요 내용 | 빌드 |
|--------|---------|------|----------|------|
| `001_tcp_socket/` | 1.0.1 | TCP 소켓 서버 | Socket 클래스, socket/bind/listen/accept | ✅ |
| `002_poll_loop/` | 1.0.2 | poll() 이벤트 루프 | Poller 클래스, 논블로킹 I/O | ✅ |
| `003_client_mgmt/` | 1.0.3 | 클라이언트 관리 | Server, Client 클래스 | ✅ |
| `004_message_parse/` | 1.0.4 | IRC 메시지 파싱 | Message 클래스, StringUtils | 📋 |
| `005_auth/` | 1.0.5 | 인증 | PASS, NICK, USER, QUIT | 📋 |
| `006_channel/` | 1.0.6 | 채널 시스템 | Channel, JOIN, PART, NAMES, LIST | 📋 |
| **`007_privmsg/`** | 1.0.7 | 메시지 전송 | PRIVMSG, NOTICE, PING, PONG | ✅ |

> ✅ = 완전 생성됨, 📋 = 노트만 생성 (007에서 역추적 가능)

---

### v2.0.0 Advanced Features (5개 스냅샷)

| 스냅샷 | 패치 ID | 이름 | 주요 내용 | 빌드 |
|--------|---------|------|----------|------|
| `008_channel_modes/` | 2.0.1 | 채널 모드 | +i, +t, +k, +o, +l | 📋 |
| `009_operator_cmds/` | 2.0.2 | 오퍼레이터 명령 | KICK, INVITE, TOPIC, MODE | 📋 |
| `010_info_cmds/` | 2.0.3 | 정보 명령 | WHOIS, WHO | 📋 |
| `011_rfc_errors/` | 2.0.4 | RFC 에러 코드 | 21개+ 에러 코드 | 📋 |
| **`012_topic_persist/`** | 2.0.5 | 토픽 영속화 | TopicPersistence | ✅ |

---

### v3.0.0 Production Infrastructure (4개 스냅샷)

| 스냅샷 | 패치 ID | 이름 | 주요 내용 | 빌드 |
|--------|---------|------|----------|------|
| `013_config/` | 3.0.1 | 설정 파일 | Config 클래스, INI 파싱 | 📋 |
| `014_logger/` | 3.0.2 | 로깅 시스템 | Logger 클래스 | 📋 |
| `015_ratelimit/` | 3.0.3 | Rate Limiting | 플러드 방지 | 📋 |
| **`016_admin_cmds/`** | 3.0.4 | 관리자 명령 | OPER, KILL, REHASH, DIE | ✅ |

> 최종 상태 (v3.0.5 ServerStats)는 루트의 `src/` 디렉토리입니다.

---

## 사용 방법

### 1. 특정 스냅샷 빌드

```bash
cd snapshots/007_privmsg
make
./ircserv 6667 password
```

### 2. 테스트

```bash
# 별도 터미널에서
nc localhost 6667
PASS password
NICK alice
USER alice 0 * :Alice
JOIN #test
PRIVMSG #test :Hello!
```

---

## 마일스톤 스냅샷 (권장 학습 경로)

가장 중요한 3개의 마일스톤 스냅샷:

1. **`007_privmsg/`** - v1.0.0 Core Functionality 완료
   - 기본 IRC 서버 동작
   - 연결, 인증, 채널, 메시지

2. **`012_topic_persist/`** - v2.0.0 Advanced Features 완료
   - 채널 모드, 오퍼레이터 명령
   - WHOIS, WHO, RFC 에러 코드

3. **루트 `src/`** - v3.0.0 Production Infrastructure 완료
   - 설정 파일, 로깅, Rate Limiting
   - 관리자 명령, 서버 통계

---

## 파일 구조

```
snapshots/
├── INDEX.md                    # 이 파일
├── 001_tcp_socket/
│   ├── Makefile
│   ├── SNAPSHOT-NOTES.md
│   └── src/
│       ├── main.cpp
│       └── network/Socket.cpp
├── 002_poll_loop/
│   └── ...
├── 003_client_mgmt/
│   └── ...
├── 007_privmsg/                # v1.0.0 완료
│   ├── Makefile
│   ├── SNAPSHOT-NOTES.md
│   └── src/
│       ├── main.cpp
│       ├── core/
│       ├── network/
│       ├── commands/
│       └── utils/
├── 012_topic_persist/          # v2.0.0 완료
│   └── ...
└── 016_admin_cmds/             # v3.0 거의 완료
    └── ...
```

---

## 관련 문서

- [`CLONE-GUIDE.md`](../CLONE-GUIDE.md) - 전체 클론코딩 가이드
- [`design/v1.0.0-core-functionality.md`](../design/v1.0.0-core-functionality.md)
- [`design/v2.0.0-advanced-features.md`](../design/v2.0.0-advanced-features.md)
- [`design/v3.0.0-production-infrastructure.md`](../design/v3.0.0-production-infrastructure.md)

---

## 스냅샷 생성 원칙

1. **빌드 가능성**: 각 스냅샷은 독립적으로 `make` 가능
2. **점진적 추가**: 이전 스냅샷의 모든 기능 포함
3. **최소 기능**: 해당 패치에서 추가된 기능만 포함
4. **테스트 가능**: 기본 테스트 시나리오 동작

---

*Generated: 2025-12-07*
