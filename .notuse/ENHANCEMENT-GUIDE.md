# 🎯 프로젝트 강화 가이드 (선택사항)

> **중요**: 현재 3개 프로젝트는 신입/주니어 수준에서 **이미 충분히 경쟁력 있음**
> 
> 아래 내용은 "더 높은 연봉" 또는 "더 좋은 회사"를 목표로 할 때만 필요

---

## 📊 현재 상태 vs 추가 작업 필요성

| 목표 | 현재 상태 | 추가 작업 |
|------|----------|----------|
| **신입 취업** | ✅ 충분 | ❌ 불필요 |
| **주니어 (연봉 5000만 이하)** | ✅ 충분 | ❌ 불필요 |
| **주니어 (연봉 6000만+)** | ⚠️ 거의 충분 | 1~2개만 |
| **게임사 대기업** | ⚠️ 기본 OK | 부하 테스트 필요 |
| **네카라쿠배** | ⚠️ 기본 OK | 배포 + 모니터링 필요 |

---

## 🎮 cpp-pvp-server 강화 가이드

### 현재 완성도: 주니어~미들급 ✅

이미 구현된 것:
- [x] 60 TPS 게임 루프
- [x] UDP 넷코드
- [x] 서버 권위 모델
- [x] 안티치트 기초
- [x] Prometheus 메트릭
- [x] 98/99 테스트 통과

### 선택적 강화 (필요시에만)

#### 1️⃣ 부하 테스트 증빙 (난이도: ⭐⭐)

**목적**: "동접 100명에서도 60 TPS 유지" 증명

```bash
# 필요 도구
brew install k6  # 또는 Artillery

# 테스트 스크립트 예시 (load_test.js)
```

```javascript
// load_test.js
import ws from 'k6/ws';
import { check } from 'k6';

export const options = {
  stages: [
    { duration: '30s', target: 50 },   // 50명까지 증가
    { duration: '1m', target: 100 },   // 100명 유지
    { duration: '30s', target: 0 },    // 종료
  ],
};

export default function () {
  const url = 'ws://localhost:8080/ws';
  const res = ws.connect(url, {}, function (socket) {
    socket.on('open', () => {
      socket.send(JSON.stringify({ type: 'join', player_id: __VU }));
    });
    socket.on('message', (msg) => {
      // 응답 시간 측정
    });
    socket.setTimeout(() => socket.close(), 60000);
  });
  check(res, { 'status is 101': (r) => r && r.status === 101 });
}
```

**결과물**: `docs/evidence/load-test-result.png` 스크린샷

**소요 시간**: 2~4시간

---

#### 2️⃣ 간단한 Unity 클라이언트 데모 (난이도: ⭐⭐⭐)

**목적**: "실제 게임처럼 동작" 시각적 증명

```
필요한 것:
- Unity 2022 LTS (무료)
- WebSocket 라이브러리 (NativeWebSocket)
- 2~3시간 작업
```

**최소 구현**:
- 2D 사각형 플레이어 2개
- WASD 이동
- 서버 상태 동기화
- 5분 데모 영상

**결과물**: `docs/evidence/unity-demo.mp4` (GIF도 가능)

**소요 시간**: 4~8시간 (Unity 경험 있으면 2~3시간)

---

#### 3️⃣ README에 아키텍처 다이어그램 추가 (난이도: ⭐)

**목적**: 면접관이 한눈에 구조 파악

```
도구: draw.io, Mermaid, Excalidraw

포함 내용:
┌─────────────┐     ┌─────────────┐
│   Client    │────▶│  WebSocket  │
│  (Unity)    │     │   Server    │
└─────────────┘     └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  GameLoop   │
                    │  (60 TPS)   │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
       ┌──────▼──────┐ ┌───▼───┐ ┌──────▼──────┐
       │ GameSession │ │ Redis │ │ PostgreSQL  │
       │  (State)    │ │(Cache)│ │   (Stats)   │
       └─────────────┘ └───────┘ └─────────────┘
```

**소요 시간**: 30분~1시간

---

## 🎬 native-video-editor 강화 가이드

### 현재 완성도: 주니어~미들급 ✅

이미 구현된 것:
- [x] FFmpeg C API 직접 사용
- [x] N-API 바인딩
- [x] WebGL 2.0 렌더링
- [x] 메모리 풀링
- [x] 19/19 테스트 통과

### 선택적 강화 (필요시에만)

#### 1️⃣ 처리 성능 벤치마크 (난이도: ⭐⭐)

**목적**: "1080p 영상 처리 시간" 정량적 증명

```bash
# 벤치마크 스크립트
#!/bin/bash
# benchmark.sh

echo "=== Video Processing Benchmark ==="

# 테스트 영상 다운로드 (Big Buck Bunny - 공개 영상)
curl -o test_video.mp4 "https://test-videos.co.uk/vids/bigbuckbunny/mp4/h264/1080/Big_Buck_Bunny_1080_10s_1MB.mp4"

# 썸네일 추출 시간 측정
echo "Thumbnail extraction (10 frames):"
time node -e "
const { ThumbnailExtractor } = require('./build/Release/video_processor.node');
const ext = new ThumbnailExtractor();
for (let i = 0; i < 10; i++) {
  ext.extractThumbnail('test_video.mp4', i * 0.5, 320, 180);
}
console.log(ext.getStats());
"

# 메타데이터 분석 시간 측정
echo "Metadata analysis:"
time node -e "
const { MetadataAnalyzer } = require('./build/Release/video_processor.node');
const analyzer = new MetadataAnalyzer();
console.log(analyzer.analyze('test_video.mp4'));
"
```

**결과물**: `docs/evidence/benchmark-result.md`

```markdown
# 벤치마크 결과

## 테스트 환경
- CPU: Apple M1 / Intel i7-10700
- RAM: 16GB
- OS: macOS 14.0 / Ubuntu 22.04

## 결과

| 작업 | 입력 | 시간 |
|------|------|------|
| 썸네일 추출 (10장) | 1080p 10초 | 0.8초 |
| 메타데이터 분석 | 1080p 10초 | 0.1초 |
| 트림 (5초) | 1080p 60초 | 2.3초 |

## 메모리 사용량
- 피크: 120MB
- 평균: 85MB
```

**소요 시간**: 1~2시간

---

#### 2️⃣ 간단한 웹 데모 페이지 (난이도: ⭐⭐)

**목적**: "실제 동작하는 에디터" 시각적 증명

```
포함 기능:
1. 파일 업로드 (드래그&드롭)
2. 썸네일 그리드 표시
3. 트림 구간 선택
4. 다운로드

기술: React + 이미 있는 백엔드
```

**결과물**: Vercel/Netlify 배포 URL 또는 데모 영상

**소요 시간**: 4~6시간

---

## ☕ spring-patterns 강화 가이드

### 현재 완성도: 주니어급 ✅

이미 구현된 것:
- [x] Spring Boot 3.x + Java 21
- [x] WebFlux + Virtual Thread
- [x] 다양한 아키텍처 패턴
- [x] 119/119 테스트 통과

### 선택적 강화 (필요시에만)

#### 1️⃣ API 문서화 - Swagger/OpenAPI (난이도: ⭐)

**목적**: "API 설계 능력" 증명

```gradle
// build.gradle에 추가
implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.3.0'
```

```yaml
# application.yml
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
```

**결과물**: `/swagger-ui.html` 접속 가능

**소요 시간**: 30분~1시간

---

#### 2️⃣ Docker Compose로 전체 스택 실행 (난이도: ⭐)

**목적**: "인프라 이해도" 증명

```yaml
# docker-compose.full.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - postgres
      - redis
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/patterns
      - SPRING_REDIS_HOST=redis

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: patterns
      POSTGRES_PASSWORD: password

  redis:
    image: redis:7-alpine

  prometheus:
    image: prom/prometheus
    volumes:
      - ./monitoring/prometheus:/etc/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
```

**결과물**: `docker-compose up`으로 전체 스택 실행

**소요 시간**: 1~2시간

---

#### 3️⃣ 간단한 부하 테스트 (난이도: ⭐⭐)

```bash
# k6 테스트
k6 run - <<EOF
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  vus: 100,
  duration: '30s',
};

export default function () {
  const res = http.get('http://localhost:8080/api/products');
  check(res, {
    'status is 200': (r) => r.status === 200,
    'response time < 200ms': (r) => r.timings.duration < 200,
  });
  sleep(0.1);
}
EOF
```

**결과물**: `docs/evidence/load-test-result.png`

**소요 시간**: 1~2시간

---

## 🎯 최종 권장사항

### 신입 목표 → 추가 작업 불필요
```
현재 상태로 지원하세요. 충분합니다.
```

### 더 좋은 조건 원하면 → 최소 작업만
```
각 프로젝트당 1개씩만 추가:

1. cpp-pvp-server → 아키텍처 다이어그램 (30분)
2. native-video-editor → 벤치마크 결과 (1시간)  
3. spring-patterns → Swagger 문서화 (30분)

총 소요 시간: 2시간
```

### 대기업/높은 연봉 목표 → 선택적 추가
```
가장 자신있는 1개 프로젝트만 집중 강화:

cpp-pvp-server 선택 시:
  - 부하 테스트 (2시간)
  - Unity 데모 (4시간)
  - 총 6시간

native-video-editor 선택 시:
  - 벤치마크 (1시간)
  - 웹 데모 (4시간)
  - 총 5시간

spring-patterns 선택 시:
  - Swagger (30분)
  - Docker Compose (1시간)
  - 부하 테스트 (1시간)
  - 총 2.5시간
```

---

## ⚠️ 하지 말아야 할 것

| ❌ 하지 마세요 | 이유 |
|---------------|------|
| 새로운 기능 추가 | 이미 충분함, 시간 낭비 |
| 코드 리팩토링 | 현재 품질 충분 |
| 테스트 더 추가 | 이미 98% 커버리지 |
| 문서 더 추가 | README/CLONE-GUIDE 충분 |

---

## 📅 실행 우선순위

```
즉시 지원 가능:
├── 현재 상태로 이력서 작성
├── GitHub README 정리 (이미 됨)
└── 포트폴리오 PDF 작성

시간 있으면 추가 (선택):
├── 아키텍처 다이어그램 (30분)
├── Swagger 문서화 (30분)
└── 벤치마크 결과 (1시간)

정말 시간 많으면 (선택):
├── 부하 테스트 (2시간)
├── Unity/웹 데모 (4-6시간)
└── 실제 배포 (2시간)
```

---

**결론**: 
- 현재 프로젝트는 **구현 난이도 자체가 주니어~미들급**
- 추가 작업은 "구현"이 아니라 **"증빙/시각화"**
- **신입 목표면 지금 바로 지원해도 됨**
- 추가 작업은 "더 좋은 조건"을 원할 때만
