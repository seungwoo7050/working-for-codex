# 019_v1.2.0-subtitle-speed 스냅샷

## 📋 패치 정보

| 항목 | 값 |
|------|-----|
| 패치 범위 | 1.2.1 ~ 1.2.4 |
| 버전 태그 | v1.2.0 |
| 이름 | Subtitle & Speed |
| 이전 단계 | v1.1.0 (Trim & Split) |

## 🎯 이 스냅샷의 목표

자막 burn-in 및 재생 속도 조절 기능을 구현합니다.

### 추가된 기능
- **SRT 자막 생성**: 표준 SubRip 형식 자막 파일 생성
- **FFmpeg 필터 체인**: `setpts` (속도), `atempo` (오디오), `subtitles` (자막)
- **SubtitleEditor 컴포넌트**: 자막 입력/편집 UI
- **ControlPanel 컴포넌트**: 속도 조절 슬라이더

## 📁 포함되어야 할 파일

### v1.1.0에서 상속
- 모든 v1.1.0 파일들

### 신규 추가 파일
```
frontend/src/components/
├── SubtitleEditor.tsx     # 자막 편집기
└── ControlPanel.tsx       # 속도/컨트롤 패널

backend/src/services/
└── ffmpeg.service.ts      # 확장: subtitle/speed 처리
```

### 수정된 파일
- `ffmpeg.service.ts`: `processVideo()` 메서드 추가
- `edit.routes.ts`: `/api/process` 엔드포인트 추가
- `App.tsx`: 새 컴포넌트 통합

## 🚀 빌드 및 실행

```bash
cd frontend && npm install && npm run dev
cd backend && npm install && npm run dev
```

## ✅ 테스트

1. 비디오 업로드
2. SubtitleEditor에서 자막 입력 (텍스트, 시작시간, 지속시간)
3. ControlPanel에서 속도 선택 (0.5x ~ 2.0x)
4. "Process" 클릭 → 자막+속도 적용된 비디오 생성

## 📝 완료 기준

- [x] 자막 burn-in 동작 (UTF-8 한글/이모지)
- [x] 0.5x ~ 2.0x 속도 조절
- [x] 자막 + 속도 동시 적용

## 📝 다음 단계

→ `026_v1.3.0-websocket-persistence/`: WebSocket 및 DB 영속성
