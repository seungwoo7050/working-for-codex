# 0.0: 프로젝트 부트스트랩 (Project Bootstrap)

## 📋 목차
- [개요](#개요)
- [1. 프로젝트 초기화](#1-프로젝트-초기화)
- [2. 프론트엔드 설정 (React + Vite)](#2-프론트엔드-설정-react--vite)
- [3. 백엔드 설정 (Node.js + Express)](#3-백엔드-설정-nodejs--express)
- [4. 로컬 개발 환경](#4-로컬-개발-환경)
- [5. Git 및 협업 설정](#5-git-및-협업-설정)
- [트러블슈팅](#트러블슈팅)

---

## 개요

native-video-editor 비디오 에디터 프로젝트의 **초기 설정 및 부트스트랩** 과정을 다룹니다.

### 목표
- React 18 + Vite 프론트엔드 프로젝트 생성
- Node.js 20 + Express 백엔드 프로젝트 생성
- TypeScript 5 strict mode 설정
- 모노레포 구조 구성
- 개발 환경 세팅

### 프로젝트 구조
```
native-video-editor/
├── frontend/          # React + Vite 프론트엔드
├── backend/           # Node.js + Express 백엔드
├── native/            # C++ Native Addon (Phase 2)
├── design/            # 설계 문서
└── README.md
```

### 기술 스택
- **프론트엔드**: React 18, TypeScript 5, Vite 5, TailwindCSS 3
- **백엔드**: Node.js 20, Express 4, TypeScript 5
- **도구**: ESLint, Prettier, tsx (TypeScript 실행기)

---

## 1. 프로젝트 초기화

### 1.1 루트 디렉토리 생성

```bash
# 프로젝트 루트 디렉토리 생성
mkdir video-editor
cd video-editor

# 하위 디렉토리 생성
mkdir frontend backend native design

# README 생성
cat > README.md << 'EOF'
# native-video-editor - Web-based Video Editor

React 18 + Node.js 20 + TypeScript 5 기반 비디오 에디터

## Quick Start

\`\`\`bash
# 프론트엔드 실행
cd frontend && npm install && npm run dev

# 백엔드 실행 (별도 터미널)
cd backend && npm install && npm run dev
\`\`\`

## 기술 스택
- Frontend: React 18, Vite 5, TailwindCSS
- Backend: Node.js 20, Express, TypeScript
EOF
```

### 1.2 Git 초기화

```bash
# Git 저장소 초기화
git init

# .gitignore 생성
cat > .gitignore << 'EOF'
# Dependencies
node_modules/
*/node_modules/

# Build outputs
dist/
*/dist/
build/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Uploads
backend/uploads/
*.mp4
*.avi
*.mov
*.mkv
*.webm

# Logs
*.log
logs/
npm-debug.log*
EOF
```

---

## 2. 프론트엔드 설정 (React + Vite)

### 2.1 Vite 프로젝트 생성

```bash
cd frontend

# Vite로 React + TypeScript 프로젝트 생성
npm create vite@latest . -- --template react-ts

# 프로젝트명, 선택사항
# Project name: video-editor-frontend
# Select a framework: React
# Select a variant: TypeScript
```

### 2.2 의존성 설치

```bash
# 기본 의존성 설치
npm install

# 추가 의존성 설치
npm install react-player        # 비디오 플레이어

# 개발 의존성 설치
npm install -D \
  tailwindcss \
  postcss \
  autoprefixer \
  prettier \
  eslint-plugin-react \
  eslint-plugin-react-hooks
```

### 2.3 package.json 설정

```json
{
  "name": "video-editor-frontend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-player": "^2.13.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "@vitejs/plugin-react": "^4.2.0",
    "autoprefixer": "^10.4.0",
    "eslint": "^8.0.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "postcss": "^8.4.0",
    "prettier": "^3.0.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

### 2.4 TypeScript 설정 (tsconfig.json)

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Path alias */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**주요 설정**:
- `strict: true`: 엄격한 타입 체크
- `noUnusedLocals: true`: 사용하지 않는 변수 에러
- `jsx: "react-jsx"`: React 17+ 자동 JSX 런타임

### 2.5 Vite 설정 (vite.config.ts)

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
      },
      '/videos': {
        target: 'http://localhost:3001',
        changeOrigin: true,
      },
    },
  },
});
```

**주요 설정**:
- **Alias**: `@/` → `src/` 경로 별칭
- **Port**: 5173 (기본 Vite 포트)
- **Proxy**: API 요청을 백엔드(3001 포트)로 프록시

### 2.6 TailwindCSS 설정

```bash
# TailwindCSS 초기화
npx tailwindcss init -p
```

**tailwind.config.js**:
```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

**src/index.css** (TailwindCSS import):
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### 2.7 디렉토리 구조

```bash
frontend/
├── public/               # 정적 파일
├── src/
│   ├── components/       # React 컴포넌트
│   │   ├── VideoUpload.tsx
│   │   ├── VideoPlayer.tsx
│   │   └── Timeline.tsx
│   ├── hooks/            # Custom Hooks
│   │   └── useVideoUpload.ts
│   ├── types/            # TypeScript 타입
│   │   └── video.ts
│   ├── utils/            # 유틸리티 함수
│   │   └── time.ts
│   ├── App.tsx           # 메인 앱 컴포넌트
│   ├── main.tsx          # 엔트리 포인트
│   └── index.css         # 글로벌 CSS
├── index.html            # HTML 템플릿
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── postcss.config.js
```

---

## 3. 백엔드 설정 (Node.js + Express)

### 3.1 npm 초기화

```bash
cd backend

# package.json 생성
npm init -y

# type: module로 변경 (ES Modules 사용)
npm pkg set type="module"
npm pkg set version="0.0.1"
npm pkg set name="video-editor-backend"
```

### 3.2 의존성 설치

```bash
# 프로덕션 의존성
npm install \
  express \
  cors \
  multer \
  fluent-ffmpeg \
  ws \
  pg \
  ioredis \
  uuid \
  sharp \
  prom-client

# 개발 의존성
npm install -D \
  typescript \
  tsx \
  @types/node \
  @types/express \
  @types/cors \
  @types/multer \
  @types/fluent-ffmpeg \
  @types/ws \
  @types/pg \
  @types/uuid \
  eslint \
  @typescript-eslint/parser \
  @typescript-eslint/eslint-plugin \
  prettier \
  jest \
  ts-jest \
  @types/jest
```

**주요 의존성**:
- **express**: 웹 프레임워크
- **multer**: 파일 업로드 (멀티파트)
- **fluent-ffmpeg**: FFmpeg wrapper (v1.1+)
- **ws**: WebSocket (v1.3+)
- **pg**: PostgreSQL 클라이언트 (v1.3+)
- **ioredis**: Redis 클라이언트 (v1.3+)
- **sharp**: 썸네일 생성 (v2.1+)
- **prom-client**: Prometheus 메트릭 (v2.3+)

### 3.3 package.json 스크립트

```json
{
  "name": "video-editor-backend",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "jest",
    "lint": "eslint . --ext ts"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "fluent-ffmpeg": "^2.1.0",
    "ioredis": "^5.3.0",
    "multer": "^1.4.0",
    "pg": "^8.11.0",
    "prom-client": "^15.1.0",
    "sharp": "^0.33.0",
    "uuid": "^13.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.0",
    "@types/express": "^4.17.0",
    "@types/fluent-ffmpeg": "^2.1.0",
    "@types/jest": "^29.5.0",
    "@types/multer": "^1.4.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "@types/ws": "^8.5.0",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "eslint": "^8.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.0.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0"
  }
}
```

**tsx**: TypeScript를 직접 실행 (ts-node보다 빠름)

### 3.4 TypeScript 설정 (tsconfig.json)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "node",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,

    /* Strict Type-Checking Options */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,

    /* Module Resolution Options */
    "types": ["node", "jest"],
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### 3.5 디렉토리 구조

```bash
backend/
├── src/
│   ├── routes/               # API 라우트
│   │   ├── upload.routes.ts
│   │   ├── video.routes.ts
│   │   └── health.routes.ts
│   ├── services/             # 비즈니스 로직
│   │   ├── storage.service.ts
│   │   └── ffmpeg.service.ts
│   ├── middleware/           # Express 미들웨어
│   │   ├── error.middleware.ts
│   │   └── logger.middleware.ts
│   ├── types/                # TypeScript 타입
│   │   └── video.ts
│   ├── utils/                # 유틸리티
│   │   └── logger.ts
│   └── server.ts             # Express 서버 엔트리
├── uploads/                  # 업로드된 파일 (git ignore)
├── package.json
├── tsconfig.json
└── .env.example
```

### 3.6 기본 Express 서버 (src/server.ts)

```typescript
import express from 'express';
import cors from 'cors';

const app = express();
const PORT = process.env.PORT || 3001;

// 미들웨어
app.use(cors());
app.use(express.json());
app.use(express.static('uploads'));

// Health check
app.get('/health', (req, res) => {
  res.json({ status: 'OK', timestamp: new Date().toISOString() });
});

// 서버 시작
app.listen(PORT, () => {
  console.log(`native-video-editor Backend running on port ${PORT}`);
});
```

### 3.7 환경 변수 (.env.example)

```bash
# Server
PORT=3001

# Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=524288000  # 500MB

# Database (v1.3+)
DATABASE_URL=postgresql://localhost:5432/video-editor

# Redis (v1.3+)
REDIS_URL=redis://localhost:6379

# FFmpeg
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe
```

---

## 4. 로컬 개발 환경

### 4.1 필수 도구 설치

```bash
# Node.js 20 설치 확인
node --version
# v20.x.x

# npm 확인
npm --version
# 10.x.x

# FFmpeg 설치 (v1.1+)
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg

# 확인
ffmpeg -version
```

### 4.2 프론트엔드 실행

```bash
cd native-video-editor/frontend

# 의존성 설치 (최초 1회)
npm install

# 개발 서버 실행
npm run dev

# 출력:
#   VITE v5.x.x ready in xxx ms
#
#   ➜  Local:   http://localhost:5173/
#   ➜  Network: use --host to expose
```

### 4.3 백엔드 실행

```bash
# 별도 터미널
cd native-video-editor/backend

# 의존성 설치 (최초 1회)
npm install

# uploads 디렉토리 생성
mkdir -p uploads

# .env 파일 생성
cp .env.example .env

# 개발 서버 실행
npm run dev

# 출력:
# native-video-editor Backend running on port 3001
# Upload directory: /path/to/backend/uploads
```

### 4.4 전체 시스템 테스트

```bash
# 1. 백엔드 Health Check
curl http://localhost:3001/health
# {"status":"OK","timestamp":"2025-11-23T12:00:00.000Z"}

# 2. 프론트엔드 접속
# 브라우저에서 http://localhost:5173 열기

# 3. 파일 업로드 테스트
# - 드래그 앤 드롭으로 비디오 파일 업로드
# - 진행률 표시 확인
# - 업로드 완료 후 재생 확인
```

### 4.5 빌드 및 프로덕션

#### 프론트엔드 빌드
```bash
cd frontend

# 프로덕션 빌드
npm run build

# dist/ 디렉토리 생성됨
ls dist/
# index.html  assets/

# 빌드 결과 미리보기
npm run preview
# http://localhost:4173
```

#### 백엔드 빌드
```bash
cd backend

# TypeScript 컴파일
npm run build

# dist/ 디렉토리 생성됨
ls dist/
# server.js  routes/  services/  ...

# 프로덕션 실행
npm start
```

---

## 5. Git 및 협업 설정

### 5.1 .gitignore

```bash
# 루트 .gitignore (이미 생성됨)
# - node_modules/
# - dist/
# - .env
# - backend/uploads/
```

### 5.2 Prettier 설정 (.prettierrc)

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2
}
```

**적용**:
```bash
# 프론트엔드 포맷팅
cd frontend
npx prettier --write "src/**/*.{ts,tsx}"

# 백엔드 포맷팅
cd backend
npx prettier --write "src/**/*.ts"
```

### 5.3 ESLint 설정 (.eslintrc.cjs)

```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true, node: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react', 'react-hooks'],
  rules: {
    '@typescript-eslint/no-unused-vars': 'error',
    '@typescript-eslint/no-explicit-any': 'error',
  },
};
```

### 5.4 Git Hooks (선택)

```bash
# Husky 설치 (pre-commit hooks)
npm install -D husky

# Husky 초기화
npx husky init

# pre-commit hook 추가
cat > .husky/pre-commit << 'EOF'
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd frontend && npm run lint
cd ../backend && npm run lint
EOF

chmod +x .husky/pre-commit
```

---

## 트러블슈팅

### 문제 1: Vite 포트 충돌
**증상**:
```
Port 5173 is already in use
```

**해결**:
```bash
# 다른 포트 사용
npm run dev -- --port 5174

# 또는 vite.config.ts에서 변경
server: {
  port: 5174,
}
```

### 문제 2: 백엔드 CORS 에러
**증상**:
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**해결**:
```typescript
// backend/src/server.ts
import cors from 'cors';

app.use(cors({
  origin: 'http://localhost:5173',
  credentials: true,
}));
```

### 문제 3: TypeScript 타입 에러
**증상**:
```
Cannot find module '@/types/video'
```

**해결**:
```json
// tsconfig.json
"baseUrl": ".",
"paths": {
  "@/*": ["src/*"]
}

// vite.config.ts도 확인
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
  },
}
```

### 문제 4: FFmpeg not found
**증상**:
```
Error: ffmpeg was not found
```

**해결**:
```bash
# FFmpeg 설치
# Ubuntu/Debian
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg

# 확인
which ffmpeg
# /usr/bin/ffmpeg

# .env 설정
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe
```

### 문제 5: 파일 업로드 크기 제한
**증상**:
```
PayloadTooLargeError: request entity too large
```

**해결**:
```typescript
// backend/src/server.ts
import express from 'express';

app.use(express.json({ limit: '50mb' }));
app.use(express.urlencoded({ limit: '50mb', extended: true }));

// multer 설정
const upload = multer({
  dest: 'uploads/',
  limits: {
    fileSize: 500 * 1024 * 1024, // 500MB
  },
});
```

### 문제 6: tsx watch 에러
**증상**:
```
Error: Cannot find module 'tsx'
```

**해결**:
```bash
# tsx 재설치
npm install -D tsx

# 또는 ts-node 사용
npm install -D ts-node
npm pkg set scripts.dev="ts-node --esm src/server.ts"
```

---

## 정리

프로젝트 **부트스트랩**을 완료했습니다:

### 프론트엔드
1. ✅ **Vite + React 18 + TypeScript 5** 프로젝트 생성
2. ✅ **TailwindCSS** 스타일링 시스템
3. ✅ **Path alias** (`@/` → `src/`)
4. ✅ **Proxy** 설정 (API 요청 백엔드로 전달)
5. ✅ **ESLint + Prettier** 코드 품질

### 백엔드
1. ✅ **Node.js 20 + Express + TypeScript** 프로젝트 생성
2. ✅ **tsx** 개발 서버 (hot reload)
3. ✅ **의존성** (multer, fluent-ffmpeg, ws, pg, ioredis 등)
4. ✅ **환경 변수** (.env) 관리
5. ✅ **디렉토리 구조** (routes, services, middleware)

### 개발 환경
1. ✅ **로컬 실행** (프론트엔드 5173, 백엔드 3001)
2. ✅ **빌드** (프로덕션 배포 준비)
3. ✅ **Git** 저장소 설정

**다음 단계**: **v1.0 - Basic Infrastructure** (비디오 업로드, 재생, 타임라인 UI 구현)
